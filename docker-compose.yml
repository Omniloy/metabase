version: '3'
services:
  omnibase:
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - '3000'
    depends_on:
      - todo-list
      - omni_test_dev-app
      - demo_dev-app
      - blank_street_dev-app
    restart: always
    networks:
      - dokploy-network
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.omnibase-example.rule=Host(`dev.omnibase.omniloy.com`)"
      - "traefik.http.routers.omnibase-example.entrypoints=websecure"
      - "traefik.http.routers.omnibase-example.tls.certResolver=letsencrypt"
      - "traefik.http.services.omnibase-example.loadbalancer.server.port=3000"
networks:
  dokploy-network:
    external: true
